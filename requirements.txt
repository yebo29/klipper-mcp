# Klipper MCP Server Dependencies
# Install with: pip install -r requirements.txt
# Compatible with Python 3.9+ (CB1/Raspberry Pi)
#
# Version caps below keep the 3.9 floor: aiohttp 3.14+, pillow 12+, and
# python-dotenv 1.2+ all raised their minimum to Python 3.10.

# Async HTTP client and server (ClientSession, web, FormData, ClientTimeout)
aiohttp>=3.13,<3.14

# .env file loading (optional but recommended - see .env.example)
python-dotenv>=1.1.1,<1.2

# Text-to-speech (optional - for TTS announcements, tools/notifications.py)
# pyttsx3>=2.99  # Uncomment if you want TTS

# Image handling (optional - for future camera snapshot processing)
# pillow>=11.3.0,<12  # Uncomment if you want camera features (12.x drops py3.9)
